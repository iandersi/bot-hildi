export type Config = {
    guild_id: bigint,
    welcome_channel: bigint,
    guest_role: bigint,
    cactpot_role: bigint,
    craftingupdates_role: bigint,
    raid_role: bigint,
    spoiler_role: bigint,
    bot_log_channel: bigint,
    static_channel: bigint,
    chat_channel: bigint,
    hildibot_config_role: bigint,
    event_role: bigint
}
